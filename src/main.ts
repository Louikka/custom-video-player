import {
    addElementToCanvas,
    clearCanvas,
    removeElementFromCanvas,
    setJumpValue
} from './lib/shared.svelte';
import VideoPlayer from './VideoPlayer.svelte';

export {
    VideoPlayer,

    setJumpValue,

    addElementToCanvas,
    removeElementFromCanvas,
    clearCanvas,
};
